export async function onRequest(context) {  const { request } = context;    // Parse URL and extract pathname  const url = new URL(request.url);  const pathname = url.pathname;    // Extract channel ID from pathname  const match = pathname.match(/^\/([^\/]+)\.m3u8$/);    if (!match) {    // Provide a helpful response for invalid URLs    return new Response('Invalid URL format. Please use: /{channel-id}.m3u8', {      status: 400,      headers: { 'Content-Type': 'text/plain' }    });  }    const id = match[1];  // M3U8 playlist content with dynamic ID  const M3U8_CONTENT = `#EXTM3U#EXT-X-STREAM-INF:AVERAGE-BANDWIDTH=830000,BANDWIDTH=1040000,RESOLUTION=1024x720,FRAME-RATE=25.000,CODECS="avc1.4d401e,mp4a.40.2",CLOSED-CAPTIONS=NONEhttps://tv.beinconnect.us/tele.php?id=${id}`;  // Handle different request methods  if (request.method === 'GET' || request.method === 'HEAD') {    // Return the M3U8 playlist with dynamic ID    return new Response(M3U8_CONTENT, {      headers: {        'Content-Type': 'application/vnd.apple.mpegurl',        'Access-Control-Allow-Origin': '*',        'Cache-Control': 'no-cache',      },    });  }  // Handle unsupported methods  return new Response('Method not allowed', {     status: 405,    headers: {      'Allow': 'GET, HEAD',      'Content-Type': 'text/plain'    },  });}